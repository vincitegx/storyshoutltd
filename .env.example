# GEMINI_API_KEY: Required for Gemini AI API calls.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
APP_URL="MY_APP_URL"

# ── Mail (Zoho SMTP) ──────────────────────────────────────────────────────────
# Same credentials as the Spring Boot backend (APP_USER_EMAIL / APP_USER_EMAIL_PWD)
MAIL_USER="david.ogbodu@storyshoutltd.com"
MAIL_PASS="ej296RM2Fsvi"

# JWT secret for admin dashboard sessions
JWT_SECRET="storyshout_super_secret_key_2026"
